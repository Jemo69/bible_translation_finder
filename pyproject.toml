[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "bible_translation_finder"
version = "0.3.0"
description = "Download Bible translations as OpenSong XML for use in FreeShow, OpenSong, and other lyrics-display software"
readme = "README.md"
requires-python = ">=3.10"
license = { text = "MIT" }
authors = [{ name = "Jemo69" }]
keywords = ["bible", "scripture", "opensong", "freeshow", "translations", "lyrics", "worship"]
classifiers = [
    "Development Status :: 4 - Beta",
    "Intended Audience :: Developers",
    "Intended Audience :: Religion",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Religion",
    "Topic :: Multimedia :: Sound/Audio :: Presentation",
    "Topic :: Software Development :: Libraries :: Python Modules",
    "Typing :: Typed",
]
dependencies = [
    "beautifulsoup4>=4.12",
    "requests>=2.31.0",
]

[project.urls]
Homepage = "https://github.com/Jemo69/bible_translation_finder"
Repository = "https://github.com/Jemo69/bible_translation_finder"

[project.scripts]
btm = "btm.cli:run_cli"

[tool.setuptools.packages.find]
where = ["src"]

[dependency-groups]
dev = [
    "pytest>=8.0",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
